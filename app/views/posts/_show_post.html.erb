<div id="<%= dom_id post %>">
  <h2><%= post.title.titleize %></h2> 
  <p><%= link_to image_tag(post.thumbnail, width: 750), post if post.thumbnail.attached? %></p>
  <%= post.body %>
  <p><%= link_to "Back", posts_path %></p>
</div>
