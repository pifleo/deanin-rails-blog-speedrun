<div id="<%= dom_id post %>">
  <h2><%= post.title.titleize %></h2> 
  <p><%= link_to image_tag(post.thumbnail, width: 300), post if post.thumbnail.attached? %></p>
  <p><%= link_to "Read more", post_path(post) %></p>
</div>
